import { JSX } from 'react';
export type PostProps = {
    id: string;
    title?: string;
    content?: string;
    date?: string;
    JSX?: JSX.Element;
  };
export type PostProp = {
    id: string;
    title?: string;
    content?: string;
    date?: string;
    
  };